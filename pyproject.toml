[project]
name = "simplestudio"
version = "0.1"
dependencies = [
    "customtkinter",
    "CTkMessagebox"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"



[tool.setuptools.package-dir]
"" = "src"

